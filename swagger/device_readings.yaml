post:
  tags:
  - devices
  description: Передача показаний устройства
  operationId: device_readings
  security:
  - Device: []
  responses:
    201:
      description: Created
    401:
      $ref: './response/response_401.yaml'
    422:
      $ref: './response/response_422.yaml'
  parameters:
  - $ref: './parameter/device_readings_body.yaml'